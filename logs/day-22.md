# Day 22: Sept 22, 2026 — Working with Accessible Tables and Forms Theory

### 📝 Today's Progress
Completed the **Working with Accessible Tables and Forms** theory section, learning best practices for screen reader compatibility across tabular data and form controls[cite: 13].

### 💡 Key Takeaways

**1. Accessible Table Structure**  
Reiterated using semantic tags (`<caption>`, `<thead>`, `<tbody>`, `<tfoot>`) and explicit `scope` attributes (`col` or `row`) on `<th>` elements so screen readers can announce spatial cell relationships accurately[cite: 13].

**2. Form Field & Label Association**  
Understood why every `<input>` requires an explicitly linked `<label>` (via `for` matching the input's `id`): it ensures screen readers announce the control's purpose upon focus and provides larger touch/click targets for users[cite: 13].

---
*Next up: Build a Tech Conference Schedule Table (Workshop)*
